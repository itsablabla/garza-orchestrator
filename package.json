{
  "name": "garza-agent-control-mcp",
  "version": "1.0.0",
  "description": "GarzaOS Agent Control MCP Server — exposes agent fleet tools via MCP protocol",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
